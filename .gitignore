# data plane — never committed (raw/silver contain Hansard text; see LICENSES-DATA.md)
data/

# local tooling noise
logs/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.duckdb
*.duckdb
*.duckdb.wal
.vscode/
# ops scratch area — only the dataset publish tool is tracked
scripts/*
!scripts/hf_publish.py
schemas/*.xsd
schemas/**/*.xsd
schemas/*.swagger.json
PLAN.md
# private working notes — never tracked publicly
backlog.md
