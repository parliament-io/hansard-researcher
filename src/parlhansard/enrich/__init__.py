"""Optional, pluggable enrichment: themes, embeddings, entity links, NER.

Not implemented yet (see docs/ROADMAP.md). Tier 1 analytics never depend on
anything in this package — the structural pipeline must run with zero API
keys.
"""
